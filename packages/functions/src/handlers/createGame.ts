export async function createGame(_args: Record<string, unknown>): Promise<unknown> {
  // TODO: generate gameId + joinCode + gmToken, store GAME#<id>/META with gmTokenHash,
  // status LOBBY. Return { game, gmToken }.
  throw new Error("Not implemented: createGame");
}
