export async function createRound(_args: Record<string, unknown>): Promise<unknown> {
  // TODO: verify gmToken, put ROUND#<n> (status DRAFT) and ROUND#<n>#Q#<qn> items
  // (type, text/imageKey, correctAnswers, defaultPoints).
  throw new Error("Not implemented: createRound");
}
