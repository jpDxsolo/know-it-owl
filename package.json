{
  "name": "know-it-owl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build -w @know-it-owl/frontend"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "sst": "4.17.1",
    "typescript": "^5.6.3"
  }
}
